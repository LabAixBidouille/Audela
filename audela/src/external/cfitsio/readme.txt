Microsoft Visual Studio:
* Open a command window.
* CD to the cfitsio directory
  -> Execute "vars.bat"
  In case of problem (not enough memory for
  environment), execute a new command shell
  with a large environment: 
  -> command /E:32768
* Make the libs:
  "make.bat"
* Install the libs:
  "install.bat"


